<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.css.app.qxjgl.dictionary.dao.DicBackDao">

	<select id="queryObject" resultType="com.css.app.qxjgl.dictionary.entity.DicBack">
		select * from QXJ_DIC_BACK where ID = #{value}
	</select>

	<select id="queryList" resultType="com.css.app.qxjgl.dictionary.entity.DicBack">
		select * from QXJ_DIC_BACK 
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from QXJ_DIC_BACK 
	</select>
	 
	<insert id="save" parameterType="com.css.app.qxjgl.dictionary.entity.DicBack">
		insert into QXJ_DIC_BACK
		(
			"ID", 
			"BACK_STATUS", 
			"REMARK"
		)
		values
		(
			#{id}, 
			#{backStatus}, 
			#{remark}
		)
	</insert>
	 
	<update id="update" parameterType="com.css.app.qxjgl.dictionary.entity.DicBack">
		update QXJ_DIC_BACK 
		<set>
			<if test="backStatus != null">"BACK_STATUS" = #{backStatus}, </if>
			<if test="remark != null">"REMARK" = #{remark}</if>
		</set>
		where ID = #{id}
	</update>
	
	<delete id="delete">
		delete from QXJ_DIC_BACK where ID = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from QXJ_DIC_BACK where ID in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>